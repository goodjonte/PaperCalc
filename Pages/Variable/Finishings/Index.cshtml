@page
@model PaperCalc.Pages.Variable.Finishings.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Finishings[0].FinishingName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Finishings[0].FinishingCostPerItem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Finishings) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FinishingName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FinishingCostPerItem)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
